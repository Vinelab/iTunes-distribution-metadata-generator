<div class="form-group dup-form-grp indent-1 track-genre-{{track_count}}">
     <label>Genre:</label>
    <input   type="text"
             required
             class="form-control"
             placeholder="Album Genre"
             name="album[tracks][{{track_count}}][genres][{{count}}]"
            >
</div>
